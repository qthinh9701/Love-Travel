window.addEventListener("load", function () {
  const cursor = document.querySelector("#cursor");
  console.log(cursor)
  cursor.addEventListener("mousemove", cursor);
  function cursor(e){
      mou
  }
});
